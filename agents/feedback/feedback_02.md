1. move the processing log frame above the operation frame (or below the start button. make it shorter.
2. append processing progress into the log in the format `(x/N)` where N is the latest input BV ID number for processing, x is the finished number.
3. add one more button to show existed BV list

